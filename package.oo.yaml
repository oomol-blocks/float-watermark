version: 0.0.6
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  oomol-preview: 0.1.0
name: float-watermark
description: An intelligent video floating watermark processing tool that supports multiple watermarks floating naturally on videos with different trajectories, effectively preventing content theft.
displayName: Float Watermark
repository: https://github.com/oomol-blocks/float-watermark
releaseNotes: update icon
icon: ./icon.png
